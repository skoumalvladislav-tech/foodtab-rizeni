# Stav k 22. 9. 2026 — Checklist → úkol

> **DOPLNĚNO 22. 9. odpoledne: NASAZENO.** Po tomhle hlášení Šéfík PR #60
> sloučil do `main` a pustil `db push` (všech 8 migrací, ověřeno). Tabulka
> „Co je hotové“ a oddíly „Nasazeno“/„Nenasazoval jsem“ níž popisují stav
> **před** tím — už neplatí, že se čeká na `db push`.

Navazuje na noční práci z 20.–21. 9. na větvi `komunikace-provozni-centrum`
(PR [#60](https://github.com/skoumalvladislav-tech/foodtab-rizeni/pull/60), sloučeno,
plný stav Provozního centra v `NOCNI-REPORT-KOMUNIKACE.md`). Dnešek byl jedna
věc, doporučená na konci té noci: tlačítko „Nahlásit problém“ u checklistu.

---

## Nejdřív to podstatné

**Checklist teď umí vyrobit úkol** — z jedné položky („Teplota lednice —
mimo mez“), nebo za celý běh, když se problém netýká jedné řádky. Nejde
o nový stav v databázi („problém“ neexistuje jako sloupec) — je to akce
člověka, stejná jako už existující „úkol ze zprávy“.

**Dvě věci, které jsem do návrhu nejdřív dal a pak sám vyhodil**, obě
potvrzené mutační zkouškou (schválné rozbití, které musí spadnout na TU
kontrolu, na kterou míří):

1. Samostatná kontrola „vidí checklist“ (`tasks.read`) před vlastním
   zadáním úkolu. Zbytečná — `zadat_ukol`, na který se deleguje, vyžaduje
   `tasks.manage` stejně, jen přísněji. Kontrola navíc by byla podmínka,
   kterou nejde rozbít, protože ji nic neotestuje samostatně.
2. Filtr na firmu (`tenant_id`) v triggeru, který hlídá přímý zápis vazby.
   Zbytečný — `branch_id` je v databázi globálně jedinečné (náhodné uuid),
   takže shoda POBOČKY sama dokazuje shodu firmy.

Obojí je pryč z migrace; zůstalo tam, co jde skutečně rozbít a co scénář
skutečně chytí.

**Cestou vyšlo najevo, jak dnes doopravdy fungují oprávnění** — a stálo
mě to jedno slepé zjištění (viz níž).

---

## Co je hotové

| co | commit | nasazeno |
|---|---|---|
| Migrace `20260922100000_checklist_ukol.sql` (`tasks.checklist_run_id`/`checklist_item_id`, trigger `tasks_vazba_checklistu`, `zalozit_ukol_z_checklistu`) | `fa17357` | **ano, 22. 9.** |
| Scénář `krok45_scenar.sql` (25 kontrol) + registrace v `run.sh` | `fa17357` | — (jen v repozitáři) |
| UI: `ukoly/[beh]/problem` (formulář), tlačítko „Nahlásit problém“ na obrazovce checklistu, „Odkud úkol je“ v detailu úkolu | `fa17357` | **ano** (PR #60 sloučen, Vercel) |
| Architektura a noční report aktualizované | `fa17357` | — (dokumentace) |

### Nasazeno 22. 9. odpoledne

`db push` (Šéfík, `--yes`) proběhl bez chyby přes všech 8 čekajících migrací
(3× Směny + notifikační služba + provozní centrum + realtime + přílohy +
tahle). PR #60 sloučen do `main`, Vercel deployment úspěšný. Ověřeno
`migration list --linked` i přímým dotazem přes Supabase MCP: 1 firma,
22 lidí, žádná ztráta dat, nové sloupce/tabulky přítomné a prázdné.
Podrobnosti v `NOCNI-REPORT-KOMUNIKACE.md`.

---

## Čísla — a z čeho jsou

**Scénář `krok45`: 25 kontrol.** Vlastní testy (katalog funkce a triggeru,
šťastná cesta u položky i za celý běh, kdo checklist nesmí ani vidět,
neexistující i skutečně cizí běh, podvržení vazby přímým zápisem, smazání
běhu/položky). Celá sada scénářů v PGlite: **1724 kontrol, nic nespadlo**
(oproti 1699 před dneškem).

> PGlite běží jako superuživatel — RLS ani sloupcové granty úplně
> neověří. Workflow „Databáze“ na PostgreSQL 16 pro `fa17357` **prošel**
> (55 s), stejně Vercel preview build. Rozhoduje ten běh, ne PGlite.

**Mutační zkouška migrace: 9 schválných rozbití, všech 9 zachyceno** — už
po odstranění těch dvou nadbytečných kontrol výš (viz „na co jsem
narazil“). Ten druhý filtr (na firmu v triggeru) jsem ještě předtím zkusil
rozbít samostatně: mutace přežila (scénář si toho nevšiml), a to bylo
potvrzení, že je opravdu zbytečný.

`scripts/komunikace.test.mjs`: beze změny, **200 kontrol**, `tsc --noEmit`
a `eslint` na dotčených souborech čisté.

---

## Co čeká na Šéfíka

~~Nasadit `20260922100000_checklist_ukol.sql`?~~ **Udělal — viz nahoře.**
Zbylé body (návrh úkolu modelem, přepis hlasu, klíče VAPID, kdo smí komu
psát, doba uchování zpráv, odhlášení a push) stojí beze změny
v `NOCNI-REPORT-KOMUNIKACE.md`, oddíl „Rozhodnutí pro Šéfíka“.

---

## Otázky

Žádná nová. `docs/hlaseni/otazky.md` beze změny.

---

## Na co jsem narazil a nešlo to

**Vlastní chyba v návrhu, ne v kódu — a nejdřív ji odhalil scénář, ne
mutace.** První verze funkce měla kontrolu přístupu na dvou úrovních —
nejdřív „vidí checklist“ (`tasks.read`), pak teprve „smí zadat úkol“
(`tasks.manage`, uvnitř `zadat_ukol`). Vypadalo to jako dvě obranné linie.
Šťastná cesta ve scénáři ale spadla i s ČLOVĚKEM, KTERÝ MÍT PŘÍSTUP MĚL —
měla jen `tasks.manage`, ne obě práva. Až tam mi došlo, že dvojí kontrola
by znamenala dvojí grant pro každého, kdo má zadávat úkoly, a že ta první
kontrola nic nepřidává — druhá je vždycky přísnější a spustí se vždycky.
Smazal jsem ji a mutační zkouškou (schválné odstranění) ověřil, že bez ní
scénář nic nepromešká.

**Scénář zprvu granty vůbec nedělaly nic — a nespadl na to hned.** Napsal
jsem `krok45` s tím, že zaměstnancům přidělím právo přes roli
(`memberships.role_id`, `public.roles`), přesně jako to dělají starší
scénáře z minulého týdne. Testovaný člověk pak neuspěl přesně tam, kde
uspět měl — bez jediné chyby v migraci. Ukázalo se, že **role dnes
oprávnění nedávají**: `app.has_permission` (migrace `20260909100000` z
9. 9.) čte `employee_permissions`/`position_permissions`, ne
`role_permissions`. `public.roles` zůstává v databázi jen kvůli cizímu
klíči na `memberships.role_id` — vypadá jako funkční systém práv, ale je
to už jen jméno u členství. Opravil jsem scénář (granty přes
`employee_permissions`) a zapsal si to do paměti, ať to příště nezavdá
stejnou půlhodinu hledání. Tohle je fakt o databázi, ne o mém kódu — jen
jsem ho nevěděl.

**Obrazovky jsem viděl jen v dočasném náhledu** (`app/nahled/checklist`,
mimo git, ukázková data) — desktop a mobil tmavý, oba bez přetečení.
**S přihlášeným účtem a skutečnými daty jsem to neviděl** — stejná mezera,
jaká platí pro celé Provozní centrum od minulé noci.

---

## Kde jsem skončil a co zbývá

Na hranici — checklist → úkol je hotový celý, ne rozdělaný. Co zůstává
z celého Provozního centra (beze změny od minulé noci): checklist →
problém řeší jen „vytvoř úkol“, ne „označ položku jako problémovou“ přímo
v běhu; diskuse k úkolu; E2E testy nad přihlášenou aplikací; ostatní body
z `NOCNI-REPORT-KOMUNIKACE.md`, oddíl NEHOTOVO.

Doporučení na příště: **E2E test dvou hlavních cest** (poslat zprávu →
vidět ji → vytvořit z ní úkol; odškrtnout položku checklistu → nahlásit
problém → vidět úkol v detailu) — dnes není prozkoušená ani jedna, ani
jedna z nich se nedá vyzkoušet ze scénářů samotných.

---

## Nasadil Šéfík

`db push` jsem nespustil já — v tomhle hlášení (napsaném dřív odpoledne) to
ještě platilo. Pak Šéfík PR #60 sloučil a `db push` pustil sám, ručně. Ověřil
jsem výsledek přes Supabase MCP (`list_migrations`, `get_advisors`, počty
řádků) — bez chyby, bez ztráty dat, žádný nový bezpečnostní nález.

---

# Dodatek k 22. 9. 2026 (večer) — Majiteli chodí upozornění kdykoliv

> **DOPLNĚNO 22. 9. večer: NASAZENO.** Sloučil jsem [PR #62](https://github.com/skoumalvladislav-tech/foodtab-rizeni/pull/62)
> na váš pokyn a vy jste pustil `db push`. Ověřil jsem přímo v databázi (ne
> podle hlášení) — funkce `app.zaradit_doruceni` má opravu, migrace
> `20260922120000` je v ledgeru, bezpečnostní poradci beze změny (žádný nový
> nález), žádná ztráta dat (1 firma, 19 aktivních zaměstnanců, fronta
> upozornění prázdná jak má být). Zbytek dodatku popisuje stav **před**
> nasazením — věty „čeká na `db push`" níž už neplatí.

## Nejdřív to podstatné

Nahlásil jste: „když odešlu zprávu vedení, tak mi žádné upozornění
nepřijde." Podíval jsem se do ostré databáze (jen počty a stavy, ne obsah
zpráv) a zjistil dvě různé věci:

1. **Váš test byl sám na sebe** — založil i napsal jste tu zprávu vy, a
   odesílatel se na vlastní zprávu nikdy neupozorňuje (stejně jako
   u jakékoli jiné zprávy v appce). To není chyba.
2. **Skutečná, trvalá díra byla jinde:** neurgentní upozornění se navenek
   (na telefon) pošle hned jen tomu, kdo je zrovna „na směně" (píchnutý
   příchod). **Majitel typicky nepíchá docházku vůbec** — takže by na něj
   normální zpráva vedení čekala navěky, ne jen do nejbližší směny, protože
   ta u majitele nenastane.

Opravil jsem bod 2. Rozsah jsem se zeptal přímo vás (jen vzkazy vedení, nebo
všechna upozornění majiteli?) — vybral jste **všechna**, a tak je to
napsané: majitel dnes obchází frontu „čeká na směnu" stejně, jako to už
dřív uměla jen naléhavá zpráva.

## Co je hotové

| co | commit | nasazeno |
|---|---|---|
| Migrace `20260922120000_majitel_doruceni_kdykoliv.sql` (`app.zaradit_doruceni`: majitel obchází frontu) | `3e6540c` | **ano, 22. 9. večer** |
| Scénář `krok46_scenar.sql` (6 kontrol) + registrace v `run.sh` | `3e6540c` | — (jen v repozitáři) |
| Zápis do `docs/COMMUNICATION_ARCHITECTURE.md` a `NOCNI-REPORT-KOMUNIKACE.md` | `3e6540c` | — (dokumentace) |
| PR [#62](https://github.com/skoumalvladislav-tech/foodtab-rizeni/pull/62) | — | **sloučen a nasazen** |

## Čísla — a z čeho jsou

**Scénář `krok46`: 6 kontrol.** Mimo směnu: majitel jde hned (normální
i důležitá), ne-majitel (Nikol, jinak identická příprava) pořád čeká —
dokazuje, že oprava neudělá „všem hned". Bez push předplatného frontu
neobejde ani majitel. `low` se pro majitele nemění. Na směně to funguje
jako dřív. Celá sada v PGlite: **1730 kontrol, nic nespadlo** (1724 před
dneškem večer).

**Mutační zkouška: 1 schválné rozbití** (smazána podmínka pro majitele) —
spadlo přesně na kontrolu „majitel mimo směnu: normální jde HNED", nic
jiného v celé sadě (1730 → 1724, přesně o těch 6 méně).

## Co čeká na Šéfíka

~~Nasadit devátou migraci — `db push`.~~ **Udělal — viz nahoře.**

## Otázky

Žádná nová — rozsah opravy jste už vybral přímo v konverzaci (všechna
upozornění majiteli, ne jen vedení).

## Na co jsem narazil a nešlo to

**Fixture v scénáři nejdřív spadla na cizí kontrole.** Přímý insert
zaměstnance s `je_majitel = true` spadl na „Majitele jmenuje jenom
majitel." — ne proto, že by scénář byl špatně, ale protože PGlite běží
všechny scénáře v jednom sezení a předchozí scénář (`krok45`) nechal
`test.user_id` nastavené (jen `reset role`, ne vyprázdnění proměnné).
Řešení: vyprázdnit `test.user_id` na začátku `krok46`, stejně jako to
už dělají `krok10`/`krok12`/`krok24`. Fakt o testovacím postroji, ne
o mé migraci.

**Push samotný tím nezačne fungovat.** Pořád chybí klíče VAPID na
Vercelu (řešili jsme dřív dnes) a druhý majitel v testovacích datech
(Lucie) nemá na žádném zařízení push zapnutý — to musí udělat ona sama
v appce. Tahle oprava řeší jen frontu/čekání, ne tyhle dvě nezávislé věci.

**Merge PR jsem první pokus neudělal sám** — auto mode ho zablokoval
(„Merge Without Review"). Na váš výslovný pokyn „mergi ty" to napodruhé
prošlo. A po prvním „hotovo" jsem si nasazení ověřil přímo v databázi,
ne podle hlášení — migrace tam ještě nebyla (asi `db push` proběhl
v jiném adresáři nebo bez `git pull` napřed). Napodruhé už tam byla.

## Kde jsem skončil

Hotovo celé a nasazeno — migrace, scénář, mutační zkouška, dokumentace,
`db push` ověřený přímo v databázi.

## Nenasazoval jsem

`db push` jsem nespustil — migrace `20260922120000` leží v repozitáři,
nasazuje ji Šéfík.
